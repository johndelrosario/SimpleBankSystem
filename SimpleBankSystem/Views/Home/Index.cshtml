@{
    ViewData["Title"] = "Home Page";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="jumbotron">
                <h4>Welcome to the simple bank system!</h4>
                @if(!User.Identity.IsAuthenticated)
                {
                    <div>Please register <a asp-controller="account" asp-action="register">here</a>.</div>
                }
            </div>
        </div>
    </div>
</div>