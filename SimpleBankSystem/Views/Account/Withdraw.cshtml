@model SimpleBankSystem.ViewModels.Account.Withdraw

@{
    ViewData["Title"] = "Withdraw";
}

<h2>Withdraw</h2>

<h4>Please enter your withdraw amount.</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Withdraw" asp-antiforgery="true" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @if (Model.IsSuccess.HasValue)
            {
                <div class="@(Model.IsSuccess.Value ? "text-success" : "text-danger") transaction-message">
                    @Model.Message
                </div>
            }
            <div class="form-group">
                <label asp-for="Amount" class="control-label"></label>
                <input asp-for="Amount" class="form-control" />
                <span asp-validation-for="Amount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Withdraw" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>